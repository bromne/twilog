<resources xmlns:app="http://schemas.android.com/apk/res-auto" xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppBar" parent="AppTheme">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:textColorPrimary">?android:textColorPrimaryInverse</item>
        <item name="android:textColorSecondary">?android:textColorSecondaryInverse</item>
        <item name="tabTextAppearance">?android:attr/textAppearanceLarge</item>
        <item name="tabSelectedTextColor">@android:color/primary_text_dark</item>
        <item name="tabTextColor">@android:color/secondary_text_dark</item>
    </style>

    <style name="AppTab" parent="AppBar">
        <item name="typeface">@string/font_fontawesome</item>
    </style>
</resources>
